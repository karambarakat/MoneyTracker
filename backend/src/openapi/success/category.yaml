openapi: 3.0.0
info:
  title: myPocket
  version: 1.0.0
paths: {}
components:
  responses:
    category:
      description: Ok
      content:
        application/json: 
          x-success: true
          example:
            $ref: "#/components/examples/category/value"
          schema:
            $ref: "#/components/schemas/category"
  examples:
    category:
      $ref: "../schema/category.yaml#/components/examples/category"
  schemas:
    category:  
      $ref: "../schema/category.yaml#/components/schemas/category"
