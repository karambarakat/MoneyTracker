{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "build": {
      "inputs": ["./src/*", "./index.js", "./package.json"],
      "outputs": ["./.src.zip"]
    },
    "deploy": {
      "dependsOn": ["build"]
    }
  }
}
