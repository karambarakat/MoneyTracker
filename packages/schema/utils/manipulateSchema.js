import schema from '../json-schema/index.js'
/**
 *
 * @param {string} id the `$id` of the schema needed to be transformed
 * @returns {import("json-schema").JSONSchema7}
 */
export function removeReadOnly($id) {
  return {}
}

/**
 *
 * @param {string} id the `$id` of the schema needed to be transformed
 * @returns {import("json-schema").JSONSchema7}
 */
export function partial($id) {
  return {}
}
