{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "build": {
      "inputs": ["./src/*", "./build.js", "./package.json"],
      "outputs": ["./dist/*"]
    }
  }
}
