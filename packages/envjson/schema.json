{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "JSON schema for .env.json files",
  "required": ["environments"],
  "definitions": {
    "environments": {
      "description": "list of all environments the app depends on",
      "type": "array",
      "items": {
        "type": "object",
        "required": ["key"],
        "properties": {
          "description": {
            "type": "string",
            "description": "describe the role of this environment"
          },
          "key": {
            "type": "string",
            "description": "the key used to identify this environment"
          },
          "secret": {
            "type": "boolean",
            "description": "this env will be encrypted in CI/CD or deployments"
          },
          "required": {
            "type": "boolean",
            "description": "whether the app will exit with non-zero if this key is not provided"
          }
        }
      }
    }
  }
}
