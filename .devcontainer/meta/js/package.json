{
  "name": "my-pocket",
  "version": "0.0.0",
  "description": "Easy to use money wallet",
  "private": true,
  "scripts": {
    "dev": "pnpm install --lockfile-only && devcontainer up --workspace-folder ."
  },
  "dependencies": {
    "devcontainer": "latest"
  }
}
